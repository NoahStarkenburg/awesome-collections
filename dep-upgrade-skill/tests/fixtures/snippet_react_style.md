# 19.0.0

## Breaking Changes

- Removed `ReactDOM.render()` — use `createRoot` instead.
- Removed `ReactDOM.hydrate()` — use `hydrateRoot`.
- Function components no longer accept `defaultProps`.

## New Features

- Added `useActionState` hook.
