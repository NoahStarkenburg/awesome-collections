# personal-timeline-mcp

An MCP server that aggregates your local activity (browser history, git commits,
filesystem mtimes, calendar events) into a single queryable timeline.

> "What was I doing last Tuesday at 3pm?"
> "What changed in the auth code today and what meeting prompted it?"
> "Summarize my workday on May 5."

All sources are **local-only** in v1 — no OAuth, no cloud upload. See
`PRIVACY.md` for what's read and how to wipe the index.

## Status

Scaffolding. See `.local/plans/personal-timeline-mcp.md` for the full build plan.

## License

MIT (same as the parent repo).
